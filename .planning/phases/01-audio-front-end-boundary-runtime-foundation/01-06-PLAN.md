---
phase: 01-audio-front-end-boundary-runtime-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - "02"
  - "04"
  - "05"
files_modified:
  - third_party/planum.cpp/src/planum/runtime/session/context.hpp
  - third_party/planum.cpp/src/planum/runtime/session/events.hpp
  - third_party/planum.cpp/src/planum/runtime/session/actions.hpp
  - third_party/planum.cpp/src/planum/runtime/session/sm.hpp
  - third_party/planum.cpp/tests/runtime_contract_sink.test.cpp
  - third_party/planum.cpp/examples/runtime_smoke/main.cpp
  - third_party/planum.cpp/benchmarks/runtime_state_probe.cpp
autonomous: true
requirements:
  - AUD-02
  - RUN-03
must_haves:
  truths:
    - "The session scaffold emits synthetic perception events through `planum::contract::sink` without implementing live STT, diarization, or endpointing."
    - "Examples and benchmarks can inspect both lifecycle states and sink-emitted contract events through the synthetic session seam."
    - "Runtime-state visibility now exists in both example and benchmark scaffolds, satisfying the Phase 1 debugging surface."
  artifacts:
    - "third_party/planum.cpp/src/planum/runtime/session/*` wire the contract sink into the session actor."
    - "third_party/planum.cpp/tests/runtime_contract_sink.test.cpp proves sink emission behavior."
    - "third_party/planum.cpp/examples/runtime_smoke/main.cpp and third_party/planum.cpp/benchmarks/runtime_state_probe.cpp exercise the synthetic session/contract seam."
  key_links:
    - "The session actor publishes contract events through explicit actions rather than callbacks or queue-based orchestration."
    - "The benchmark hook from Plan 04 is extended to observe contract output as well as inspectable states."
---

<objective>
Wire the normalized contract sink into the session scaffold and extend the example and benchmark hooks to observe the synthetic session/contract seam.

Purpose: Finish the front-end side of Phase 1 by proving that the lifecycle scaffold can emit normalized contract events and that both examples and benchmarks can inspect those outputs.
Output: Session sink wiring, targeted sink-emission tests, a runtime smoke example, and an upgraded benchmark probe.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-RESEARCH.md
@AGENTS.md
@docs/rules/sml.rules.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-02-PLAN.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-04-PLAN.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread `planum::contract::sink` through the session actor</name>
  <files>third_party/planum.cpp/src/planum/runtime/session/context.hpp, third_party/planum.cpp/src/planum/runtime/session/events.hpp, third_party/planum.cpp/src/planum/runtime/session/actions.hpp, third_party/planum.cpp/src/planum/runtime/session/sm.hpp, third_party/planum.cpp/tests/runtime_contract_sink.test.cpp</files>
  <behavior>
    - Test 1: Entering lifecycle states emits `runtime_state_changed` events through the sink.
    - Test 2: Synthetic partial, final, endpoint, degraded, and error transitions emit the corresponding contract events.
    - Test 3: Wiring the sink does not add backend logic, retention policy, or queue-based dispatch.
  </behavior>
  <action>Update the session scaffold to accept a `planum::contract::sink` in context and emit synthetic perception events from explicit SML actions per D-06 and D-12. Keep the events synthetic and bounded: this plan proves the session/contract seam, not live speech intelligence. Add a targeted sink-emission test that verifies the event mapping and protects against backend or policy creep.</action>
  <verify>
    <automated>cmake --build third_party/planum.cpp/build --target planum_tests -j &amp;&amp; ctest --test-dir third_party/planum.cpp/build --output-on-failure -R runtime_contract_sink</automated>
  </verify>
  <done>The session actor publishes synthetic contract events through the sink, and automated tests prove the emission behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the example and benchmark hooks to observe the session/contract seam</name>
  <files>third_party/planum.cpp/examples/runtime_smoke/main.cpp, third_party/planum.cpp/benchmarks/runtime_state_probe.cpp</files>
  <action>Add a deterministic runtime smoke example and extend the benchmark probe so both drive the synthetic session actor, inspect current lifecycle states, and capture sink-emitted contract events. Keep both hooks backend-free and deterministic. They should demonstrate the seam for future work, not perform live audio, endpointing, or diarization.</action>
  <verify>
    <automated>cmake --build third_party/planum.cpp/build --target runtime_smoke runtime_state_probe -j &amp;&amp; third_party/planum.cpp/build/examples/runtime_smoke/runtime_smoke &amp;&amp; third_party/planum.cpp/build/benchmarks/runtime_state_probe</automated>
  </verify>
  <done>Both example and benchmark scaffolds exercise the synthetic session/contract seam and expose inspectable state plus sink output for later runtime work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session actor -> contract sink | Synthetic lifecycle output crosses from internal orchestration into a public front-end handoff seam |
| example/benchmark hooks -> runtime scaffold | Debug surfaces must observe state and events without becoming an alternate runtime path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | T | `third_party/planum.cpp/src/planum/runtime/session/actions.hpp` | mitigate | Task 1 emits only typed synthetic contract events from bounded actions with no backend calls or policy decisions |
| T-01-17 | D | `third_party/planum.cpp/tests/runtime_contract_sink.test.cpp` | mitigate | Task 1 proves sink emission behavior so broken or missing event wiring is caught automatically |
| T-01-18 | I | `third_party/planum.cpp/examples/runtime_smoke/main.cpp` | mitigate | Task 2 keeps example and benchmark hooks synthetic so they surface state and events without implying live-runtime completeness |
</threat_model>

<verification>
Run `runtime_contract_sink`, then build and execute the runtime smoke example and the benchmark probe. Verification fails if the sink is not exercised in tests or if the example/benchmark hooks require a live backend.
</verification>

<success_criteria>
The session actor emits normalized synthetic contract events, and both example and benchmark hooks can inspect the lifecycle and the contract seam without live runtime implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-06-SUMMARY.md`
</output>
