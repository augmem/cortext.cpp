---
phase: 01-audio-front-end-boundary-runtime-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - third_party/planum.cpp/src/planum/runtime/signaling/context.hpp
  - third_party/planum.cpp/src/planum/runtime/signaling/events.hpp
  - third_party/planum.cpp/src/planum/runtime/signaling/guards.hpp
  - third_party/planum.cpp/src/planum/runtime/signaling/actions.hpp
  - third_party/planum.cpp/src/planum/runtime/signaling/sm.hpp
  - third_party/planum.cpp/benchmarks/runtime_state_probe.cpp
autonomous: true
requirements:
  - AUD-01
  - RUN-03
must_haves:
  truths:
    - "The `signaling` child actor exists in the same locked folder pattern as the other runtime actors."
    - "A benchmark-facing scaffold can inspect the locked lifecycle states without needing live audio or backend logic."
    - "Examples and benchmarks now have a stable entrypoint for lifecycle-state inspection per roadmap success criterion 5."
  artifacts:
    - "third_party/planum.cpp/src/planum/runtime/signaling/{context,events,guards,actions,sm}.hpp exist."
    - "third_party/planum.cpp/benchmarks/runtime_state_probe.cpp drives the synthetic session lifecycle and records inspectable states."
  key_links:
    - "The benchmark probe instantiates the session actor from Plan 02 and uses synthetic events only."
    - "The benchmark scaffold stays backend-free while proving RUN-03 state visibility for later runtime work."
---

<objective>
Add the `signaling` child-actor landing zone and the first benchmark-facing lifecycle probe.

Purpose: Finish the locked actor decomposition and satisfy the missing benchmark/debug hook for RUN-03 before contract or bridge work proceeds.
Output: A scaffold-only `signaling` actor plus a benchmark probe that inspects the session lifecycle through synthetic events.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-RESEARCH.md
@AGENTS.md
@docs/rules/sml.rules.md
@third_party/planum.cpp/docs/sml.rules.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `signaling` child-actor landing zone</name>
  <files>third_party/planum.cpp/src/planum/runtime/signaling/context.hpp, third_party/planum.cpp/src/planum/runtime/signaling/events.hpp, third_party/planum.cpp/src/planum/runtime/signaling/guards.hpp, third_party/planum.cpp/src/planum/runtime/signaling/actions.hpp, third_party/planum.cpp/src/planum/runtime/signaling/sm.hpp</files>
  <action>Create the `signaling` actor folder in the locked `emel.cpp` component split per D-11 and D-13. Keep it scaffold-only with synthetic events and no-op actions. Do not implement live signaling transport, UI callbacks, or cross-thread messaging; this landing zone exists to reserve ownership and file structure only.</action>
  <verify>
    <automated>test -f third_party/planum.cpp/src/planum/runtime/signaling/context.hpp &amp;&amp; test -f third_party/planum.cpp/src/planum/runtime/signaling/events.hpp &amp;&amp; test -f third_party/planum.cpp/src/planum/runtime/signaling/guards.hpp &amp;&amp; test -f third_party/planum.cpp/src/planum/runtime/signaling/actions.hpp &amp;&amp; test -f third_party/planum.cpp/src/planum/runtime/signaling/sm.hpp &amp;&amp; rg -n "namespace planum::runtime::signaling|struct context|struct model" third_party/planum.cpp/src/planum/runtime/signaling/*.hpp</automated>
  </verify>
  <done>The `signaling` actor landing zone exists in the locked folder pattern and remains scaffold-only.</done>
</task>

<task type="auto">
  <name>Task 2: Add a benchmark-facing lifecycle probe</name>
  <files>third_party/planum.cpp/benchmarks/runtime_state_probe.cpp</files>
  <action>Create a benchmark/smoke harness that instantiates the Plan 02 session actor, drives it with synthetic lifecycle events, and records the current state set through `visit_current_states(...)` or `is(...)` per RUN-03. Keep it backend-free and deterministic. The benchmark’s job is state visibility and debugging scaffolding, not performance measurement against live audio yet.</action>
  <verify>
    <automated>cmake --build third_party/planum.cpp/build --target runtime_state_probe -j &amp;&amp; third_party/planum.cpp/build/benchmarks/runtime_state_probe</automated>
  </verify>
  <done>A benchmark-facing hook exists that can inspect the locked lifecycle states through synthetic session transitions without any live runtime backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session actor -> benchmark probe | Debug/benchmark hooks must observe state without mutating runtime behavior |
| signaling scaffold -> future integrations | The signaling actor must not become an ad hoc callback or queue layer in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | D | `third_party/planum.cpp/benchmarks/runtime_state_probe.cpp` | mitigate | Task 2 uses synthetic events and explicit state inspection only, with no live backend or timing loops |
| T-01-11 | T | `third_party/planum.cpp/src/planum/runtime/signaling/*` | mitigate | Task 1 keeps signaling scaffold-only and forbids callback storage, queues, or transport logic |
| T-01-12 | I | `third_party/planum.cpp/benchmarks/runtime_state_probe.cpp` | mitigate | The benchmark reports lifecycle state only and does not invent retention or memory-policy semantics |
</threat_model>

<verification>
Run the signaling folder structure check, then build and execute `runtime_state_probe`. Verification fails if the benchmark cannot inspect the locked states or if signaling introduces queue or backend behavior.
</verification>

<success_criteria>
The `signaling` actor exists in the locked pattern, and a benchmark-facing lifecycle probe provides stable RUN-03 state inspection scaffolding.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-04-SUMMARY.md`
</output>
