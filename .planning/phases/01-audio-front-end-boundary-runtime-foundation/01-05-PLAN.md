---
phase: 01-audio-front-end-boundary-runtime-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - third_party/planum.cpp/include/planum/contract/event_kind.hpp
  - third_party/planum.cpp/include/planum/contract/ids.hpp
  - third_party/planum.cpp/include/planum/contract/runtime_state.hpp
  - third_party/planum.cpp/include/planum/contract/perception_event.hpp
  - third_party/planum.cpp/include/planum/contract/sink.hpp
  - third_party/planum.cpp/tests/contract_perception_event.test.cpp
autonomous: true
requirements:
  - AUD-00
  - AUD-02
must_haves:
  truths:
    - "The `planum.cpp -> cortext` boundary is defined as normalized perception-event types rather than callback-heavy example structs."
    - "The contract carries transcript, timing, segment, speaker, lifecycle, degraded, and error metadata without memory-policy fields."
    - "Automated tests prove the contract shape and explicitly guard against retention/write-policy drift."
  artifacts:
    - "third_party/planum.cpp/include/planum/contract/perception_event.hpp defines the canonical normalized event payload."
    - "third_party/planum.cpp/include/planum/contract/sink.hpp defines the output sink seam."
    - "third_party/planum.cpp/tests/contract_perception_event.test.cpp proves the contract taxonomy and exclusions."
  key_links:
    - "The contract state enum aligns with the locked lifecycle while remaining front-end owned."
    - "Tests check for absence of memory-policy/backend fields instead of only compiling the headers."
---

<objective>
Define the canonical normalized perception contract and prove its shape in targeted tests.

Purpose: Lock the repo-to-repo seam before any sink wiring or Cortext bridge work starts, keeping Cortext audio-engine agnostic per D-02 through D-07.
Output: Public `planum::contract` headers and a contract-focused test that validates required fields and forbidden policy drift.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-RESEARCH.md
@AGENTS.md
@docs/rules/sml.rules.md
@include/cortext/cortext.hpp
@examples/chat/voice_session.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the normalized contract types</name>
  <files>third_party/planum.cpp/include/planum/contract/event_kind.hpp, third_party/planum.cpp/include/planum/contract/ids.hpp, third_party/planum.cpp/include/planum/contract/runtime_state.hpp, third_party/planum.cpp/include/planum/contract/perception_event.hpp</files>
  <action>Create the contract types required by D-02, D-04, D-05, D-06, and D-07: event taxonomy, ids, runtime-state enum, and `perception_event`. Include fields for transcript text, timestamps, segment/turn identity, speaker metadata, lifecycle state, degraded status, and error status. Do not include retention flags, write-policy controls, backend model paths, or callback types.</action>
  <verify>
    <automated>test -f third_party/planum.cpp/include/planum/contract/perception_event.hpp &amp;&amp; rg -n "partial|final|endpoint|runtime|degraded|error|speaker|segment|turn" third_party/planum.cpp/include/planum/contract/*.hpp</automated>
  </verify>
  <done>The boundary types cover the full locked perception surface and stay free of memory-policy or backend-specific ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the sink seam and contract-shape tests</name>
  <files>third_party/planum.cpp/include/planum/contract/sink.hpp, third_party/planum.cpp/tests/contract_perception_event.test.cpp</files>
  <behavior>
    - Test 1: The contract represents partial transcript, final transcript, endpoint reached, runtime state changed, degraded, and error events.
    - Test 2: The contract carries transcript text, timestamps, ids, and speaker metadata required by D-06.
    - Test 3: The contract excludes retention/write-policy fields, backend model configuration, and example callback types.
  </behavior>
  <action>Define the output-only sink interface and add a targeted contract test that proves the event taxonomy and explicitly guards against memory-policy drift. Use the current chat example only as taxonomy input; do not let `VoiceSessionConfig` or callback types become part of the contract surface.</action>
  <verify>
    <automated>cmake --build third_party/planum.cpp/build --target planum_tests -j &amp;&amp; ctest --test-dir third_party/planum.cpp/build --output-on-failure -R contract_perception_event</automated>
  </verify>
  <done>The contract has a stable sink seam, and automated tests prove both the required metadata shape and the forbidden field exclusions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `planum.cpp` contract -> downstream consumers | Boundary types must carry only perception facts, not Cortext memory authority |
| example taxonomy -> canonical contract | Existing chat example shapes can leak callback-heavy or backend-specific surface into the contract if copied directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | E | `third_party/planum.cpp/include/planum/contract/*` | mitigate | Task 1 excludes retention and write-policy fields so front-end types cannot assume Cortext memory authority |
| T-01-14 | T | `third_party/planum.cpp/include/planum/contract/perception_event.hpp` | mitigate | Task 2 adds tests that assert required metadata and forbid backend/callback field drift |
| T-01-15 | I | `third_party/planum.cpp/include/planum/contract/sink.hpp` | mitigate | Keep the seam output-only so contract consumers cannot mutate session ownership through the sink interface |
</threat_model>

<verification>
Build `planum_tests` and run `contract_perception_event`. Verification fails if the contract omits required event types or smuggles in memory-policy/backend ownership.
</verification>

<success_criteria>
`planum.cpp` exposes a canonical normalized boundary contract, and automated tests prove both the required event taxonomy and the absence of policy drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-05-SUMMARY.md`
</output>
