---
phase: 01-audio-front-end-boundary-runtime-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - third_party/planum.cpp/src/planum/runtime/session/context.hpp
  - third_party/planum.cpp/src/planum/runtime/session/events.hpp
  - third_party/planum.cpp/src/planum/runtime/session/guards.hpp
  - third_party/planum.cpp/src/planum/runtime/session/actions.hpp
  - third_party/planum.cpp/src/planum/runtime/session/sm.hpp
  - third_party/planum.cpp/tests/runtime_session.test.cpp
autonomous: true
requirements:
  - AUD-01
  - RUN-01
  - RUN-03
must_haves:
  truths:
    - "The top-level `co_sm` session actor exposes the locked lifecycle states `inactive`, `activating`, `listening`, `segmenting`, `endpointing`, `signaling`, `degraded`, and `errored`."
    - "Synthetic lifecycle events progress through explicit SML transitions rather than queues, mailboxes, or self-dispatch."
    - "The locked lifecycle is inspectable in automated tests via `is(...)` or `visit_current_states(...)`."
  artifacts:
    - "third_party/planum.cpp/src/planum/runtime/session/sm.hpp defines the top-level session machine."
    - "third_party/planum.cpp/src/planum/runtime/session/{context,events,guards,actions}.hpp provide the session actor structure."
    - "third_party/planum.cpp/tests/runtime_session.test.cpp proves the locked-state behavior."
  key_links:
    - "The session machine uses `co_sm` and explicit `sml::unexpected_event` handling per D-08, D-09, and D-10."
    - "Automated tests exercise both state inspection and unexpected-event behavior instead of only compiling the target."
---

<objective>
Add the top-level `co_sm` session actor skeleton and prove the locked lifecycle in targeted tests.

Purpose: Land the inspectable runtime owner early so later child-actor, contract, and benchmark work composes with a stable top-level state machine.
Output: Session actor source files plus a behavior test that exercises the locked lifecycle and unexpected-event handling.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-RESEARCH.md
@AGENTS.md
@docs/rules/sml.rules.md
@third_party/planum.cpp/docs/sml.rules.md

<interfaces>
Locked state set from D-10:

```text
inactive, activating, listening, segmenting,
endpointing, signaling, degraded, errored
```

Required interaction style from D-08 and D-09:

```text
Use stateforward/sml.cpp with co_sm, synchronous RTC dispatch,
explicit transitions, and explicit unexpected-event handling.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the session actor structure and lifecycle events</name>
  <files>third_party/planum.cpp/src/planum/runtime/session/context.hpp, third_party/planum.cpp/src/planum/runtime/session/events.hpp, third_party/planum.cpp/src/planum/runtime/session/guards.hpp</files>
  <action>Create the session actor structure in the `emel.cpp` component pattern per D-11: persistent context, synthetic lifecycle event types, and pure guards for the locked state graph. Keep the events scaffold-only and RTC-safe per D-08 through D-10. Do not add timers, backend callbacks, device ownership, or event-queue fields to context.</action>
  <verify>
    <automated>test -f third_party/planum.cpp/src/planum/runtime/session/context.hpp &amp;&amp; test -f third_party/planum.cpp/src/planum/runtime/session/events.hpp &amp;&amp; test -f third_party/planum.cpp/src/planum/runtime/session/guards.hpp &amp;&amp; rg -n "inactive|activating|listening|segmenting|endpointing|signaling|degraded|errored" third_party/planum.cpp/src/planum/runtime/session/*.hpp</automated>
  </verify>
  <done>The session actor has explicit state/event/guard structure and contains the full locked lifecycle vocabulary before actions or test execution are added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the `co_sm` session machine and prove lifecycle behavior</name>
  <files>third_party/planum.cpp/src/planum/runtime/session/actions.hpp, third_party/planum.cpp/src/planum/runtime/session/sm.hpp, third_party/planum.cpp/tests/runtime_session.test.cpp</files>
  <behavior>
    - Test 1: A new session starts in `inactive` and can be inspected through `is(...)` or `visit_current_states(...)`.
    - Test 2: Synthetic activation, segmentation, endpoint, signaling, degrade, and error events stay within the locked state set.
    - Test 3: Unexpected external events are handled explicitly without queues, mailboxes, or self-dispatch.
  </behavior>
  <action>Implement the top-level machine with `co_sm` per D-08 and D-09, using destination-first transition rows, explicit `sml::unexpected_event` handling, and bounded no-op actions only. Add a targeted test that proves the locked-state progression and explicit unexpected-event behavior. Do not add real endpointing, speaker attribution, or STT logic; this plan is the lifecycle skeleton only.</action>
  <verify>
    <automated>cmake --build third_party/planum.cpp/build --target planum_tests -j &amp;&amp; ctest --test-dir third_party/planum.cpp/build --output-on-failure -R runtime_session</automated>
  </verify>
  <done>The session machine is implemented with `co_sm`, its behavior is proven by automated tests, and the locked lifecycle is inspectable in code rather than implied by comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external control events -> session actor | Synthetic lifecycle events can drive invalid or unstable state progression if not modeled explicitly |
| session machine -> future runtime work | The top-level actor must remain RTC-safe so later child actors and sinks do not inherit queue semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | D | `third_party/planum.cpp/src/planum/runtime/session/sm.hpp` | mitigate | Use bounded explicit transitions with no queues or self-dispatch in Task 2 |
| T-01-05 | T | `third_party/planum.cpp/src/planum/runtime/session/events.hpp` | mitigate | Keep event types synthetic and typed so only the locked lifecycle is expressible in Phase 1 |
| T-01-06 | E | `third_party/planum.cpp/src/planum/runtime/session/actions.hpp` | mitigate | Restrict actions to bounded scaffold-only behavior with no backend calls, timers, or context-driven branching |
</threat_model>

<verification>
Build `planum_tests` and run `runtime_session`. Verification fails if the locked states are missing, if unexpected events are not tested, or if the actor relies on queue-like orchestration.
</verification>

<success_criteria>
The top-level session actor exists, uses `co_sm`, and is covered by automated behavior tests that prove the locked lifecycle and explicit unexpected-event handling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-02-SUMMARY.md`
</output>
