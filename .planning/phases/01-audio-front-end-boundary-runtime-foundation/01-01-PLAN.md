---
phase: 01-audio-front-end-boundary-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - third_party/planum.cpp/CMakeLists.txt
  - third_party/planum.cpp/README.md
  - third_party/planum.cpp/tests/CMakeLists.txt
  - third_party/planum.cpp/examples/CMakeLists.txt
  - third_party/planum.cpp/benchmarks/CMakeLists.txt
  - third_party/planum.cpp/tests/repo_scaffold_smoke.cpp
autonomous: true
requirements:
  - AUD-01
  - RUN-01
must_haves:
  truths:
    - "`planum.cpp` configures and builds as its own scaffold repo before any live audio-runtime implementation lands."
    - "The Phase 1 scaffold has dedicated test, example, and benchmark build hooks for later plans."
    - "The repo documentation states that Phase 1 is scaffolding-first and forbids queue-based or backend-specific runtime work."
  artifacts:
    - "third_party/planum.cpp/CMakeLists.txt wires a minimal runtime library plus test/example/benchmark subdirectories."
    - "third_party/planum.cpp/tests/CMakeLists.txt, third_party/planum.cpp/examples/CMakeLists.txt, and third_party/planum.cpp/benchmarks/CMakeLists.txt exist as stable landing zones."
    - "third_party/planum.cpp/tests/repo_scaffold_smoke.cpp provides a deterministic repo bootstrap smoke test."
  key_links:
    - "The top-level build graph exposes reusable hooks so later plans do not need to restructure the repo again."
    - "README scope language keeps D-12 intact by preventing backend/device/runtime creep in Phase 1."
---

<objective>
Bootstrap `planum.cpp` as a standalone scaffold repo with stable build hooks for the actor, contract, example, and benchmark work that follows.

Purpose: Satisfy D-01 and D-12 up front so the remaining Phase 1 plans can land in fixed repo structure instead of mixing setup with implementation.
Output: A buildable `planum.cpp` scaffold with test/example/benchmark subdirectories and a repo smoke test.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-RESEARCH.md
@AGENTS.md
@docs/rules/sml.rules.md
@third_party/planum.cpp/README.md
@third_party/planum.cpp/docs/sml.rules.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the `planum.cpp` scaffold build graph</name>
  <files>third_party/planum.cpp/CMakeLists.txt, third_party/planum.cpp/tests/CMakeLists.txt, third_party/planum.cpp/examples/CMakeLists.txt, third_party/planum.cpp/benchmarks/CMakeLists.txt</files>
  <action>Create a standalone CMake project for `planum.cpp` per D-01 that exposes a minimal runtime target plus stable subdirectory hooks for tests, examples, and benchmarks. Keep the graph scaffold-only per D-12: no audio devices, model runtimes, worker threads, queues, or mailbox helpers. Wire the subdirectories so later plans can add new files without another repo-layout rewrite.</action>
  <verify>
    <automated>cmake -S third_party/planum.cpp -B third_party/planum.cpp/build -DCMAKE_BUILD_TYPE=Debug &amp;&amp; cmake --build third_party/planum.cpp/build --target planum_runtime -j</automated>
  </verify>
  <done>`planum.cpp` configures independently, exposes the runtime target, and has dedicated build hooks for tests, examples, and benchmarks.</done>
</task>

<task type="auto">
  <name>Task 2: Document scaffold scope and add a repo smoke test</name>
  <files>third_party/planum.cpp/README.md, third_party/planum.cpp/tests/repo_scaffold_smoke.cpp</files>
  <action>Update `README.md` so it explicitly implements D-01, D-04, D-05, D-08, and D-12: `planum.cpp` is the primary repo for the audio front-end, Phase 1 is scaffolding-first, `stateforward/sml.cpp` is required, and retention or memory semantics do not belong here. Add a deterministic smoke test that proves the repo-level scaffold target, test harness, and CTest registration work before actor or contract logic lands.</action>
  <verify>
    <automated>cmake --build third_party/planum.cpp/build --target planum_runtime planum_tests -j &amp;&amp; ctest --test-dir third_party/planum.cpp/build --output-on-failure -R repo_scaffold_smoke</automated>
  </verify>
  <done>The repo documents the locked phase boundary, and automated smoke coverage proves the scaffold is executable rather than a dead directory tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo scaffold -> later runtime work | The Phase 1 scaffold can accidentally pull runtime/backend concerns into the wrong repo layer |
| build/test hooks -> future plans | Missing or unstable hooks would force later plans to re-open repo structure instead of using fixed landing zones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `third_party/planum.cpp/CMakeLists.txt` | mitigate | Keep dependencies minimal and explicitly forbid backend/device/runtime additions in Task 1 so the scaffold cannot quietly grow into Phase 2 work |
| T-01-02 | D | `third_party/planum.cpp/tests/CMakeLists.txt` | mitigate | Add deterministic smoke coverage in Task 2 so broken harness wiring is caught before later plans depend on it |
| T-01-03 | E | `third_party/planum.cpp/README.md` | mitigate | Document D-04 and D-05 explicitly so front-end ownership does not expand into Cortext memory semantics |
</threat_model>

<verification>
Configure `planum.cpp`, build the runtime and test targets, and run `repo_scaffold_smoke`. Verification fails if the repo cannot build independently or if the scaffold language permits live runtime/backend work in Phase 1.
</verification>

<success_criteria>
`planum.cpp` is a buildable scaffold repo with reusable test/example/benchmark hooks, and the repo documentation clearly locks Phase 1 to scaffold-only front-end work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-01-SUMMARY.md`
</output>
