---
phase: 01-audio-front-end-boundary-runtime-foundation
plan: 07
type: execute
wave: 3
depends_on:
  - "05"
files_modified:
  - src/audio/planum_bridge.hpp
  - src/audio/planum_bridge.cpp
  - tests/audio_planum_bridge.test.cpp
  - tests/CMakeLists.txt
  - examples/chat/CMakeLists.txt
  - examples/chat/planum_bridge_smoke.cpp
autonomous: true
requirements:
  - RUN-02
must_haves:
  truths:
    - "Cortext can consume finalized `planum.cpp` perception events through a private bridge without changing `include/` or the C API."
    - "Partial, lifecycle, degraded, and error events have explicit no-write behavior in Phase 1."
    - "A deterministic Cortext-side example hook exists for the bridge without making `voice_session.hpp` the contract boundary."
  artifacts:
    - "src/audio/planum_bridge.hpp and src/audio/planum_bridge.cpp define the non-public bridge."
    - "tests/audio_planum_bridge.test.cpp proves bridge mapping behavior inside `cortext_tests`."
    - "examples/chat/planum_bridge_smoke.cpp provides a deterministic bridge smoke target."
  key_links:
    - "The bridge routes finalized transcript events into existing `Cortext::ProcessTextAt(...)` only."
    - "No change is made to `include/cortext/*` or `include/cortext/capi.h`."
---

<objective>
Add the private Cortext bridge for finalized perception events and a deterministic Cortext-side smoke hook.

Purpose: Complete the Phase 1 boundary by proving the new front-end contract composes with Cortext through private source files only, preserving public API stability.
Output: A private bridge under `src/audio/`, a white-box bridge test, and a small chat-side smoke target.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-RESEARCH.md
@AGENTS.md
@docs/rules/sml.rules.md
@include/cortext/cortext.hpp
@include/cortext/capi.h
@examples/chat/voice_session.hpp
@.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and test the private `planum.cpp -> cortext` bridge</name>
  <files>src/audio/planum_bridge.hpp, src/audio/planum_bridge.cpp, tests/audio_planum_bridge.test.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Test 1: `final_transcript` events route through existing `Cortext::ProcessTextAt(...)` with deterministic source-id derivation.
    - Test 2: Partial, lifecycle, endpoint-only, degraded, and error events do not route through `ProcessAudio(...)` and do not imply durable writes in Phase 1.
    - Test 3: The bridge stays private to `src/` and requires no changes in `include/cortext/*` or the C API.
  </behavior>
  <action>Create a private bridge under `src/audio/` that accepts `planum::contract::perception_event` and translates only finalized transcript-bearing events into existing `Cortext::ProcessTextAt(...)` calls per D-02, D-03, and D-07. Add a white-box test to `cortext_tests` that proves the mapping behavior and explicitly protects against `ProcessAudio(...)` routing or public API drift.</action>
  <verify>
    <automated>cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug &amp;&amp; cmake --build build --target cortext_tests -j &amp;&amp; ctest --test-dir build -R cortext_tests --output-on-failure</automated>
  </verify>
  <done>The bridge is private to Cortext source files, finalized events reuse existing timestamped text ingestion, and automated tests cover the no-write behavior for non-final events.</done>
</task>

<task type="auto">
  <name>Task 2: Add a deterministic bridge smoke target in the chat example area</name>
  <files>examples/chat/CMakeLists.txt, examples/chat/planum_bridge_smoke.cpp</files>
  <action>Add a small smoke target in `examples/chat/` that constructs synthetic `planum::contract::perception_event` values and runs them through the private bridge. Keep `voice_session.hpp` unchanged and informational only. The smoke target exists to prove the seam for future example work, not to replace the full chat app or introduce live audio behavior.</action>
  <verify>
    <automated>cmake --build build --target cortext_planum_bridge_smoke -j</automated>
  </verify>
  <done>Cortext has a deterministic example-side bridge hook that compiles independently and does not require public API or contract-surface changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `planum::contract::perception_event` -> Cortext private bridge | Untrusted front-end metadata crosses into Cortext internals |
| bridge -> stable Cortext APIs | The bridge must not widen or replace the existing public API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | T | `src/audio/planum_bridge.cpp` | mitigate | Task 1 restricts bridge handling to explicit finalized transcript events and keeps non-final events no-write |
| T-01-20 | E | `include/cortext/*`, `include/cortext/capi.h` | mitigate | Task 1 explicitly forbids public header/C API edits and tests for private-source-only integration |
| T-01-21 | I | `examples/chat/planum_bridge_smoke.cpp` | mitigate | Task 2 keeps the example deterministic and synthetic so it does not imply live speech-runtime completeness |
</threat_model>

<verification>
Build and run `cortext_tests`, then build `cortext_planum_bridge_smoke`. Verification fails if the bridge requires public API changes, if it routes non-final events through Cortext writes, or if the example hook depends on live audio/runtime code.
</verification>

<success_criteria>
Cortext remains audio-engine agnostic at the public surface, finalized planum contract events can reach existing text ingestion through a private bridge, and a deterministic example hook exists for later integration work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-front-end-boundary-runtime-foundation/01-07-SUMMARY.md`
</output>
