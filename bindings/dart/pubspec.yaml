name: cortext
description: Dart FFI bindings for the Cortext memory engine.
version: 1.2.1
homepage: https://github.com/augmem/cortext
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  ffi: ^2.1.4
  meta: ^1.17.0

dev_dependencies:
  ffigen: ^19.1.0
  lints: ^6.0.0
  test: ^1.25.15
